import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Enable static HTML export (replaces the removed `next export` command)
  output: 'export',
};

export default nextConfig;
